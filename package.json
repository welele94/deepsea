{
  "name": "deepsea",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "parcel index.html --host 0.0.0.0 --port 3000 --open",
    "dev": "parcel index.html --host 0.0.0.0 --port 3000 --open",
    "build": "parcel build index.html --dist-dir dist --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.15.4",
    "typescript": "^5.9.2"
  }
}
